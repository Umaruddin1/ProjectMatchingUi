# API mode: local | server
VITE_API_MODE=local

# Local backend URL (for local development)
VITE_API_URL_LOCAL=http://localhost:8000

# Hosted backend URL (Vercel)
VITE_API_URL_SERVER=https://project-matching-service.vercel.app

# Optional legacy fallback (used only when VITE_API_MODE is not local/server)
VITE_API_URL=http://localhost:8000
